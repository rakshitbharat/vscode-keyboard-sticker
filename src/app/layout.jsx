export const metadata = {
  title: "VSCode Keyboard Shortcuts",
  description: "Interactive keyboard layout showing VSCode shortcuts",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
